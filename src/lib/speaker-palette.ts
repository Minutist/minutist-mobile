/**
 * Speaker palette size — the number of --speaker-N tokens defined in
 * styles/theme.css.  A unit test (speaker-palette.test.ts) asserts the two
 * agree, so changing one without the other fails the suite.  Mirrors the
 * desktop's transcript/speaker-color.ts.
 */
export const SPEAKER_PALETTE_SIZE = 8;
