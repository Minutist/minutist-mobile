/**
 * CaptureView — stub placeholder.
 *
 * The record control and quick-notes field will be wired here once the native
 * recorder plugin (T2) and sync stub (T3) are in place.
 */
export function CaptureView() {
  return (
    <div className="view-body" aria-label="Capture view">
      <p style={{ color: 'var(--stone)', fontFamily: 'var(--font-text)' }}>
        Capture — coming soon.
      </p>
    </div>
  );
}
