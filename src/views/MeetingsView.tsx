/**
 * MeetingsView — stub placeholder.
 *
 * The read-only synced-meeting list (synced notes, transcripts, summaries)
 * will be wired here once the sync stub (T3) is in place.
 */
export function MeetingsView() {
  return (
    <div className="view-body" aria-label="Meetings view">
      <p style={{ color: 'var(--stone)', fontFamily: 'var(--font-text)' }}>
        Meetings — coming soon.
      </p>
    </div>
  );
}
